// Placeholder: the Phase 2A medium acceptance spec instructs the writer to
// replace this with a real `getStatus()` implementation returning
// `{ ok: boolean; version: string }`. Do not import from this file in
// production code — it is fixture content.

export function getStatus(): { ok: boolean; version: string } {
  return { ok: false, version: "0.0.0" };
}
