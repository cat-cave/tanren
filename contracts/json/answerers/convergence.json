{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "assessment": {
      "enum": [
        "progress",
        "stalled",
        "velocity_defer"
      ],
      "type": "string"
    },
    "blockingRootCauseId": {
      "description": "Stable id/root-cause label of the worst-severity merge-gating finding (NOT surface text), so the same blocker is recognized across loops. Empty when there is no blocking finding.",
      "type": "string"
    },
    "blockingRootCauseProgress": {
      "enum": [
        "retired",
        "reduced",
        "unchanged",
        "regressed",
        "none"
      ],
      "type": "string"
    },
    "reasoning": {
      "description": "Cite the BLOCKING root cause + the finding-delta across loops: which root causes were retired, which recurred, and whether the total P-score is decreasing.",
      "minLength": 1,
      "type": "string"
    }
  },
  "required": [
    "assessment",
    "blockingRootCauseId",
    "blockingRootCauseProgress",
    "reasoning"
  ],
  "type": "object",
  "x-tanren-schema-id": "tanren.convergence_answer.v1"
}
