# Tanren Runner

This is the hello-world runner image. It proves the runner container and SSH daemon boundary.

Real agent CLIs (`claude-code`, `codex`, `opencode`, `ccusage`, `codexbar`) are intentionally not installed in this milestone.
